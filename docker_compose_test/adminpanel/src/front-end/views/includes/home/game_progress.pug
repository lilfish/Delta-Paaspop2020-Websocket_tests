div.card(class=current_game==true ? "display-none" : "")
	small.float-right (ik luister naar OSC)
	h3.card-title Op het moment is er geen game gestart 
	hr
	div.row
		div.col.col-lg-6 
			h6 Acties
			button(onClick="start_game('"+next_game._id+"')").button.primary-button spel starten
		div.col.col-lg-6=next_game

div.card(class=current_game==true ? "" : "display-none")
	div.row
		h3.card-title.col.col-lg-6 Op het moment speelt 
			strong spel 1
		small.col.col-lg-6.align-right (ik luister naar OSC)
	hr
	div.row
		div.col
			div.progress-bar.striped.animated
				span.progress-bar-green(style={width: '80%'})
